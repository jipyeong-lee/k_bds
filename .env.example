# 이 파일을 .env 로 복사해 실제 값을 채운다 (.env 는 커밋되지 않는다).
# 사용법: set -a; . ./.env; set +a
ORCH_BASE_URL=https://<플랫폼-주소>
ORCH_PAT=pat_<웹UI /ui/users.html → Access tokens 에서 발급>
ORCH_USER=<웹 UI 계정>
ORCH_PASSWORD=<웹 UI 비밀번호>
