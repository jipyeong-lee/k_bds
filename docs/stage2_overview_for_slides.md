# Stage-2 범용 RLVR — 발표용 정리

**최초 작성 2026-08-02 · 최종 갱신 2026-08-04 · 🚨 step ~900 학습 붕괴로 체인 취소(step 1,046) · 홀드아웃 전량 1,772 짝지음 7지점 반영**

> 이 문서는 **발표자료용 자립 요약**이다. 절 하나가 슬라이드 한 장에 대응하도록 구성했고,
> 다른 문서를 읽지 않아도 되게 필요한 수치를 모두 안에 담았다.
> 개발용 상세는 [`stage2_experiments.md`](stage2_experiments.md)(실험 이력) ·
> [`stage2_data.md`](stage2_data.md)(데이터 상세) · [`stage2_run73924_progress.md`](stage2_run73924_progress.md)(중간 점검) 참조.

---

## 1. 한 장 요약

**의료 멀티모달 추론 모델을 4단계로 만든다. ② 범용 RLVR 본실행은 step ~900에서 출력 형식이 무너져 붕괴했고 08-04에 취소했다. 그러나 붕괴 직전까지는 정상적으로 개선되고 있었다 — step 850이 최고점(홀드아웃 51.52%, init 대비 +8.18pp p<0.0001, step400 대비 +2.48pp p=0.020). 실패한 것은 학습이 아니라 안정성이고, 근인 후보(`overlong_filter`가 잘린 completion을 손실·KL 양쪽에서 제외)까지 특정했다. 목표 도메인인 의료는 여섯 지점 전부 무변화 — 붕괴와 무관하게 Stage-3의 근거가 굳어졌다.**

| | |
|---|---|
| 하는 일 | 검증가능 정답으로 GRPO 강화학습 — 일반 시각추론 + math + 의료를 균형 강화 |
| 데이터 | 3개 소스 실측 선별 → **74,787건**(일반 53 / math 20 / 의료 26), 클린 홀드아웃 1,772 |
| 방법 | **dr_grpo + GDPO** (GRPO 계열 5종 clean A/B 승자) · LoRA r16 · 8×A100 |
| 진행 | **800 / 2,337 step (34%)** · 오류 0건 · 73924 → 73925 체인 인계 정상 |
| **성과** | 전량 1,772 짝지음: init **43.34% → step850 51.52%** = **+8.18pp, p<0.0001**. step400 대비도 **+2.48pp, p=0.020** |
| **사고** | 🚨 **step ~900 형식 붕괴 → 정책 자유낙하.** step1000 은 25.56% 로 init 보다 낮다. 08-04 체인 취소(step 1,046) |
| **판단** | **학습이 아니라 안정성의 실패다.** 붕괴 직전까지 개선 중이었고 게이트를 넘길 속도였다. 최고 체크포인트 확보 |
| **다음** | **파라미터 수정 후 재시작** — 근인 후보 `overlong_filter=True`(잘린 completion 을 손실·KL 에서 제외) |

---

## 2. 파이프라인에서의 위치

```
① 콜드스타트 SFT  →  ② 범용 RLVR  →  ③ 의료 특화 RL(RaR)  →  ④ 평가
   ✅ 완료             🚀 지금 여기        ⏳ 미시작(핵심 산출물)     🔄 기준선 확보
   형식 주입           검증가능 정답        개방형 의료 VQA          HealthBench
   LoRA SFT           GRPO/GDPO           GRPO + 루브릭 judge
```

- **② 의 입력** = ① 의 산출 `sft_mixed_merged`(v3 콜드스타트). 이 문서에서 **`init`(RL 0%)** 로 부른다.
- **② 의 역할** = 정답이 기계적으로 검증되는 문제로 **추론 습관을 다지는** 단계.
  깊은 의료 추론은 ③(개방형·judge 채점)이 전담한다. ②를 의료 MC로 과적하면 패턴매칭을 학습할 위험이 있어 의료 비중을 26%로 제한했다.

---

## 3. 방법론 — 왜 dr_grpo + GDPO 인가

### 계보: 각 논문이 직전 방법의 무엇을 고쳤나

| 방법 | 겨냥한 한계 | 해결책 |
|---|---|---|
| **GRPO** (2402.03300) | PPO의 **가치망(critic)** 이 정책망만큼 커서 메모리·연산 부담 | critic 제거. 한 문제에 답안 여러 장을 뽑아 **그룹 내 상대 보상**을 advantage 로 |
| **DAPO** (2503.14476) | 엔트로피 붕괴 · 후반 gradient 소실 · 길이 폭주 | Clip-Higher · **Dynamic Sampling** · token-level loss · overlong shaping |
| **Dr.GRPO** (2503.20783) | GRPO 의 두 편향 — ÷시퀀스길이(**길이 편향**), ÷그룹std(**난이도 편향**) | 두 정규화 **삭제** → unbiased 추정 |
| **GSPO** (2507.18071) | 토큰 단위 IS 의 장문 **분산 누적** | IS 를 **시퀀스 단위**로 |
| **GDPO** (2601.05242) | **다중 보상** 가중합 후 통짜 정규화 → 조합이 같은 advantage 로 붕괴 | 결합 **전에 보상별 개별 z-score** → 보상 간 상대차 보존 |

### 5종을 3개 손잡이의 좌표로 본다

ms-swift 가 노출하는 **직교 3-knob** 조합으로 정리하면 외울 것이 없다.

| 손잡이 | 무엇을 바꾸나 | 선택지 |
|---|---|---|
| `loss_type` | 손실 정규화·클리핑 | `grpo`(÷길이) · `dapo`(÷배치토큰+clip-higher) · **`dr_grpo`**(÷상수) |
| `importance_sampling_level` | IS 단위 | **`token`** · `sequence`(=GSPO) |
| `scale_rewards` | advantage 정규화 | `group`(÷std) · `none`(=dr_grpo) · **`gdpo`**(보상별 개별 z-score) |

**채택 = `(dr_grpo, token, gdpo)`.** dr_grpo 가 A/B 승자였고, GDPO 는 거기서 `scale_rewards` 하나만 바꾼 1-knob 변형이다.

### clean A/B 결과 (기법 외 전 조건 동일, 한 스크립트에서 env 토글)

| 방법 | 결과 |
|---|---|
| GRPO (baseline) | plateau — Acc 0.500 정점 후 정체, zero_std 0.24→0.33 |
| DAPO | 안정(zero_std 0)하나 **미돌파** Acc 0.465, 길이 재폭주 |
| **dr_grpo** ✅ | **돌파·승자** Acc **0.526**, 길이 억제 |
| **GDPO** ✅ | dr_grpo 의 1-knob 변형, 멀티리워드 균형 → **본실행 채택** |
| GSPO | 열세 |

> ⚠️ 위 수치는 **구 데이터셋·구 홀드아웃** 기준이다. 현재 확장셋 수치(§7)와 같은 그래프에 올리면 안 된다.

---

## 4. 데이터셋 — "이름·규모로 안 고르고 전부 받아서 실측했다"

### 스크리닝: 8개 소스를 다운로드해 직접 측정

| 소스 | 규모 | 실측에서 드러난 것 | 판정 |
|---|---:|---|---|
| **DeepVision-103K** | 102,531 | MC 과반·고유답 8% → 정보량 낮아 step600 포화 원인 | ✅ 기존 base |
| **MMK12** | 15,616 | **82% math_verify**, 100% 순수 math, 고유답 32% — 최고 품질 | ✅ **채택** |
| **PMC-VQA** | 329,536 | 4지선다, 정답 letter 균형(B/C/A/D), 고유이미지 29,133/30K | ✅ **채택** |
| ThinkLite-VL-hard | 11,031 | 61% 짧은문자열·Yes/No 11% — 노이즈 | ⚠️ 드롭 |
| ~~Kvasir-VQA~~ | 58,849 | **고유이미지 671개**(한 장에 17문항)·고유답 107 | ❌ degenerate |
| ~~SLAKE~~ | 4,919 | **이미지 450개**뿐 | ⏸ 스킵 |
| ~~VQA-RAD~~ | 1,793 | 극소 | ⏸ |
| ~~PathVQA~~ | 19,654 | 절반이 개방형 → 검증 불가 | ⏸ |

**교훈: 규모와 다양성은 따로 논다.** Kvasir 는 58,849건이지만 고유 이미지가 671개다. 이름과 건수만 보고 골랐다면 그대로 실패했다.

### 최종 구성 (2026-07-24 재조립)

| | 소스 | 건수 | 비중 | 역할 |
|---|---|---:|---:|---|
| **train** | DeepVision (서브샘플) | 40,000 | 53% | 일반 시각추론 base |
| | MMK12 (전량) | 15,204 | 20% | 순수 math — 측정된 유일 약점 직격 |
| | PMC-VQA (서브샘플) | 19,583 | 26% | 의료 광범위 MC — 목표 정렬 |
| | **합계** | **74,787** | | |
| **holdout** | DeepVision 972 + MMK12 400 + PMC-VQA 400 | **1,772** | | `_source` 태그로 층별 분리 평가 |

- **누수 0 검증**: 전 소스 **이미지 바이트해시 dedup**. 구 DeepVision 홀드아웃의 22% 오염도 이때 최종 해소(train 에서 453건 제외).
- **비율이 중요하고 절대량은 아니다**: GRPO 는 셔플 샘플링이라 자연비율(DeepVision 80%)이면 롤아웃 80%가 이미 포화된 데이터가 된다. 실제로 본실행 655 step 에서 소비한 프롬프트는 **5,240건**(step 당 8개)으로, 74,787 을 다 보지도 않는다.
- **보상 견고성 실측**: `accuracy_mix` 가 "맞지만 형식 다른" 답을 깎는지 12 케이스 시험 → **0/12 손실**(콤마·분수·단위·대소문자·Yes/No 전부 정답 처리).

---

## 5. 학습 세팅

### 하드웨어 제약이 방법을 정했다

| | |
|---|---|
| 노드 | **8 × A100 80GB PCIe**, **NVLink 없음**(`nvidia-smi topo -m` 전부 PHB) → GPU P2P 차단, NCCL 이 호스트 RAM(SHM) 폴백 |
| 결과 | full fine-tuning 은 18GB gradient all-reduce 병목 → 9B 모델인데도 **375~660초/step** |
| **대응** | **전 단계 LoRA** — adapter gradient(수십 MB)만 통신 → **약 128초/step**, 5~75배 개선. base 동결이라 DeepSpeed 없이 DDP 로 충분 |
| OS | CentOS 7.9 / glibc 2.17 → 최신 vLLM 설치 불가 → **컨테이너 스택**(2026-07-27 apptainer 파손 후 loader 우회 운용) |

### 실행 하이퍼파라미터 (job 73924 실제 값)

| 구분 | 값 |
|---|---|
| 베이스 / init | Qwen3.5-9B (`qwen3_5`) / v3 콜드스타트 `sft_mixed_merged` |
| 튜닝 | LoRA **rank 16 · alpha 32 · all-linear** · bf16 |
| 알고리즘 | **dr_grpo 손실 + GDPO** · IS `token` · ε 0.2 · KL **β 0.04** |
| 보상 | `accuracy_mix` **1.0** + `format_think` **0.2** + `soft_overlong` **0.2** |
| 샘플링 | `num_generations` **4** · temperature **0.9** · `enable_thinking` |
| 필터 | `dynamic_sample`(재샘플 최대 3) · `overlong_filter` |
| 배치 | pdtbs 1 × accum 4 × 8 GPU = **32 completions/step → 프롬프트 8/step** |
| 길이 | `max_completion_length` 6,144 · `soft_cache_length` 2,048 · `max_length` 10,240 |
| 최적화 | lr **1e-5** · gradient checkpointing · flash-attn |
| 롤아웃 | vLLM **colocate** · TP 1 · gpu_mem_util 0.4 · sleep_level 1 |
| 스케줄 | `max_steps` **2,337** · save_steps 50 · save_total_limit 5 |

**출력 형식**: `<think>간결 추론</think><answer>최종답</answer>` (`\boxed{}` 미사용).
내장 `accuracy` 보상은 객관식 letter 를 파싱하지 못해 DeepVision 의 약 48%에서 보상이 통째로 소실된다 →
**커스텀 `accuracy_mix`**(수식은 math_verify, letter·문자열은 정규화 일치)로 분기 처리했다.

> ⚠️ **`max_steps=2,337`은 1 epoch 이 아니라 0.25 epoch 이다.**
> GRPO 에서 `per_device_train_batch_size` 는 프롬프트가 아니라 completion 을 센다 →
> 프롬프트/step = 32 ÷ `num_generations`(4) = **8** → 1 epoch = 74,787 ÷ 8 = **9,348 step**.
> 종전 문서의 "1 epoch = 2,337 step" 은 `÷ num_generations` 누락이었다(2026-08-02 정정).
> 진짜 1 epoch 은 약 6,694 노드시간 = 예산의 134% 로 애초에 실행 불가능하다.

---

## 6. 학습 진행 경과

![Stage-2 확장셋 GDPO 학습 곡선](assets/stage2_expanded_73924_curves.png)

**step 800 / 2,337 (34%) · 294 s/it(벽시계) · 73924 → 73925 체인 인계 정상 · OOM·CUDA error·Traceback 0건**
*(2026-08-03 11:00 KST. 73924 는 TimeLimit 로 step 787 에서 종료, 73925 가 checkpoint-750 에서 재개)*
*`step_time` 은 147 s/it 이지만 롤아웃 생성·체크포인트가 그 밖에 있어 벽시계는 그 2배다 — 일정 계산엔 294 를 쓴다.*

### 구간 대조 (1~100 step 평균 → 최근 100 step 평균)

| 지표 | 초반 | 최근 | 변화 | 읽기 |
|---|---:|---:|---:|---|
| rewards/AccuracyMix | 0.4248 | 0.4495 | **+5.8%** | 대부분 길이 구성효과 (아래 참조) |
| rewards/FormatThink | 0.9461 | 0.9583 | +1.3% | 천장 부근 회복 |
| completions/mean_length | 1,127 | 1,176 | +4.3% | ✅ 401~500 정점 1,660 에서 되돌아옴 |
| completions/clipped_ratio | 4.9% | 3.9% | **−21.8%** | ✅ 401~500 정점 9.8% 에서 회복 |
| kl | 0.0036 | 0.0313 | +765% | 절대값 0.031 로 작음 — 정책이 꾸준히 이동 중 |
| entropy/mean | 0.5377 | 0.5227 | −2.8% | 다양성 붕괴 없음 |

> ⚠️ **이 표는 2026-08-02 판(step 551~650)에서 뒤집혔다.** 당시 "정확도 정지(+1.3%)·길이 팽창(+28.1%)·클리핑 증가(+41.4%)"로
> 읽었으나, 그것은 **200~500 구간의 일시적 길이 인플레이션**을 정상 상태로 오인한 것이다. 700 step 대에 세 지표 모두 되돌아왔다.

### 학습 로그로 답하려 했으나, 거기서도 답이 안 나온다

`log_completions` 가 남긴 `completions.jsonl` 에는 문항별 채점 결과가 있다 — **24,000 문항, 홀드아웃 n=300 의 80배**.
이걸로 "더 학습하면 오르나"를 답할 수 있을 것으로 봤는데, 결과는 아니었다.

![소스별 학습 정확도 추세](assets/stage2_source_trend.png)

**① 최근 상승은 대부분 길이 구성이다.** 장문 completion 은 정답률이 훨씬 낮다(39.4% vs 44.6%).
그 비중이 11.0% → 16.8% → **11.9%** 로 갔다 오면서 평균이 저절로 오르내렸다.
원 계열의 최근 점프 +2.4pp 중 장문 제외 후 남는 것은 **+0.8pp**(t=0.64, 미검출).

**② 결정에 필요한 "지금부터의 기울기"는 판별되지 않는다.**

| 소스 | step 301~750 기울기 (장문 제외) | 잔여 1,587 step 외삽 |
|---|---|---|
| deepvision | −0.047 pp/100step (t=−0.11) | **−0.7pp** [95% **−14.4 ~ +12.9**] |
| mmk12 수학 | +0.195 (t=+0.17) | +3.1pp [−31.7 ~ +37.9] |
| pmcvqa 의료 | +0.906 (t=+1.23) | +14.4pp [−8.5 ~ +37.2] |

세 소스 모두 0 을 크게 포함한다. **표본 24,000 건으로도 그렇다** —
스텝 간 분산이 커서 기울기의 유효 표본은 "완성문 24,000" 이 아니라 "스텝 450 개"에 가깝기 때문이다.
정확도의 **수준**을 재는 것과 **기울기**를 재는 것은 다른 문제이고, 결정에 필요한 것은 후자다.

> 엔트로피는 안 꺾였고 KL 도 작아 정책 자체는 건강하다. 문제는 모델이 아니라 **측정**이다.
> *(교란요인 확인: `dataset_shuffle=True` — 학습셋이 소스순 정렬이라 필수 / 최종 epoch 0.25 라 문항 재등장 없음 = fresh-sample / 재개 구간 751~787 중복 제거)*

**자원**: `step_time` 합계 29.0h 대 실제 경과 58.2h → **절반이 학습 밖**(vLLM 롤아웃 생성·재샘플링·체크포인트). GRPO colocate 의 구조적 비용.

---

## 7. 중간 홀드아웃 평가 — 전량 1,772 짝지음 (2026-08-04)

![Stage-2 홀드아웃 전량 평가](assets/stage2_holdout_paired.png)

확장 홀드아웃을 **전량 1,772건**(deepvision 972 / mmk12 400 / pmcvqa 400) 사용해
`init·400·500·600·700` 다섯 지점을 **동일 문항·greedy(temperature 0)** 로 채점하고,
문항별 점수를 저장해 **짝지음(McNemar)** 으로 비교했다. 검출 하한이 **8.0pp → 1.7~2.3pp**.

| (%) | **init** | 400 | 500 | 600 | 700 | **850** | 1000 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **전체** (n=1,772) | **43.34** | 49.04 | 49.55 | 49.32 | 50.40 | **51.52** | **25.56** |
| deepvision 일반 (n=972) | 35.60 | 42.59 | 43.11 | 42.08 | 44.44 | **46.40** | 22.84 |
| mmk12 수학 (n=400) | 48.25 | 56.25 | 58.25 | 59.25 | 56.75 | 57.50 | **49.75** |
| **pmcvqa 의료** (n=400) | 57.25 | 57.50 | 56.50 | 57.00 | 58.50 | 58.00 | **8.00** |
| format(`<answer>`) | 0.947 | 0.892 | 0.887 | 0.887 | 0.903 | 0.895 | **0.180** |
| mean_chars | 1,812 | 2,124 | 2,103 | 2,126 | 2,031 | 2,107 | **7,893** |

| 짝지음 Δ | 전체 | deepvision | 수학 | 의료 |
|---|---|---|---|---|
| init → 400 | **+5.70pp** p<0.0001 ✅ | +7.00 ✅ | +8.00 ✅ | +0.25 ✗ |
| **init → 850** | **+8.18pp** p<0.0001 ✅ | +10.80 ✅ | +9.25 ✅ | +0.75 ✗ |
| 400 → 700 (300 step) | +1.35pp p=0.18 ✗ | +1.85 ✗ | +0.50 ✗ | +1.00 ✗ |
| **400 → 850 (450 step)** | **+2.48pp p=0.020 ✅** | +3.81 ✅ | +1.25 ✗ | +0.50 ✗ |
| 400 → 1000 (붕괴 후) | **−23.48pp** p<0.0001 🚨 | −19.75 | −6.50 | **−49.50** |

### 읽기

**① RL 은 확실히 듣는다.** init → 400 **+5.70pp, p<0.0001**. 같은 비교가 n=300 에서는
"+3.67pp, p=0.428, 판정불가"였다 — 효과가 없던 게 아니라 **±9.8pp 노이즈에 묻혀 있었다.**
(콜드스타트 SFT 의 base 0.2500 → init 0.4533 = **+20.3pp, p<0.001** 도 여전히 유효하다. 단 그 값은 n=300 층화 기준이다.)

**② "step 400 이후 정체"는 오독이었다.** 300 step 간격(400→700)에서는 +1.35pp 로 미검출이지만
**450 step 간격(400→850)에서는 +2.48pp, p=0.020 으로 검출된다.** 정체가 아니라 **분해능 부족**이었고,
앞서 제시한 95% 구간 [−0.6, +3.3]pp 안에 정확히 들어온다. **붕괴 직전까지 정상적으로 개선되고 있었다.**

**②-b 🚨 step ~900 에서 붕괴했다.** 출력 형식이 무너지고(`FormatThink` 0.90 → 0.06) 정책이 자유낙하해
step 1000 은 **25.56%** — init 보다 낮다. 08-04 에 체인을 취소했다(step 1,046 정지).
손상이 소스별로 극단적인 것은 채점 방식 때문이다 — 의료는 `<answer>A</answer>` 가 있어야 letter 를 뽑으므로
**letter 층이 0.5835 → 0.0798**, 수학은 본문에서 숫자를 잡아내 −6.5pp 에 그친다.
즉 −23pp 는 능력 상실보다 **형식 상실**에 가깝다(실사용에서는 구분이 무의미하다). 상세 = 진행 보고서 §8.

**③ 의료는 여섯 지점 전부 움직이지 않았다.** init 대비 +0.25 / −0.75 / −0.25 / +1.25 / +0.75pp, 모두 p>0.5.
n=100(±9.8pp) 시절의 "0.57 → 0.53 하락"은 **재현되지 않았다** — 하락이 아니라 **무변화**다.
반면 수학은 +8~11pp 로 뚜렷하다. **검증가능 정답 RLVR 이 수학은 밀어주지만 의료 MC 는 못 밀어준다**는 그림이
n=400 에서도, 붕괴와 무관하게도 그대로다.

**④ 형식·길이는 붕괴 전부터 대가를 치르고 있었다.** format **0.947 → 0.89**(−6pp), mean_chars **1,812 → ~2,100**(+16%).
전체 정확도가 오르는 와중에도 누적됐고, **결국 이 방향으로 무너졌다**(②-b).

**⑤ 실패한 것은 학습이 아니라 안정성이다.** 400→850 이 +2.48pp(p=0.020)로 검출됐다는 것은
**사전등록 게이트(800 step 간격 +3pp)를 넘길 만한 속도였다**는 뜻이다. 남은 과제는
"더 학습하면 오르나"가 아니라 **"어떻게 안 무너지게 하나"** 로 바뀌었다.

> ⚠️ 그림 왼쪽 패널의 **점별 오차막대는 비짝지음**이라 서로 겹친다 — 그걸 보고 "차이 없다"고 읽으면 안 된다.
> 판정축은 가운데·오른쪽 짝지음 패널이다.
> ⚠️ 전량은 소스 가중이 **55/23/23** 이라 층화 n=300(각 33.3%)의 전체값과 **직접 비교 불가**다.
> init 이 0.4533(n=300) vs 0.4334(전량)로 다른 것은 가중치 차이지 측정 오차가 아니다. 소스별 수치는 언제나 비교 가능하다.

---

## 8. 현재 판단과 다음 단계

### 판단 — 학습은 되고 있었다. 무너진 건 안정성이다.

**끝난 질문 ① "RL 이 효과가 있는가":** 있다. init → 850 **+8.18pp, p<0.0001**.

**끝난 질문 ② "step 400 이후로도 오르는가":** 오른다. 400 → 850 **+2.48pp, p=0.020**.
간격이 300 step 일 때는 미검출(+1.35pp)이었으나 450 step 에서 검출됐다 — **정체가 아니라 분해능 부족이었다.**
08-03 에 나열했던 "중단 쪽 신호" 중 홀드아웃 근거는 전부 철회된다.

| 08-03 의 중단 쪽 신호 | 최종 판정 |
|---|---|
| 홀드아웃 step 400 이후 미검출(폭 1.0pp) | ❌ 철회 — 하한이 8.0pp 였을 뿐. **400→850 +2.48pp p=0.020** |
| 학습측 reward 정지·길이 팽창 | ⚠️ **길이 팽창은 진짜였다** — 결국 이 방향으로 붕괴했다 |
| 목표 도메인(의료) 상승 없음 | ✅ **유지** — n=400·여섯 지점에서도 무변화 |
| 구 데이터셋 step 600 포화 선례 | ❌ 철회 — 확장셋은 850 까지 올랐다 |
| RLVR 장기학습 diversity collapse 경고(2606.15455) | ⚠️ **적중** — 엔트로피 0.55 → 0.10 붕괴가 실제로 일어났다 |

**새 질문 — "어떻게 안 무너지게 하나":** step ~900 에서 형식이 무너지며 정책이 자유낙하했다(§7 ②-b).
방아쇠는 정방향 되먹임이고, 유력한 근인은 **`overlong_filter=True` 가 잘린 completion 을
손실·KL 양쪽에서 제외**해 앵커를 무력화한 것이다. 상세·기각된 가설 4종 = 진행 보고서 §8.

**변하지 않은 제약 — 의료:** 여섯 지점 전부 무변화다. Stage-2 를 얼마나 더 돌리든
목표 도메인이 따라 오르리라는 기대는 **근거를 잃었다.** 붕괴와 무관하게 **Stage-3 의 우선순위를 올린다.**

> ⚠️ **판정축은 전체(3 소스 합성)로 한다.** 08-03 초판은 "deepvision 만 유의하게 오른다"를 근거로 그 층을
> 판정축으로 삼았으나 **그 근거가 철회됐다**. 층별 우열에 사전 근거가 없는 상태에서 한 층을 미리 고르면
> 그 선택 자체가 사후 해석이 된다. 이 사전기준은 **재시작 런에 그대로 재사용한다.**

### 다음 단계

| # | 조치 | 내용 |
|---|---|---|
| ~~**0**~~ | 🚨 **학습 취소 (08-04 10:14)** | 73925·73926·73927. 의존성이 `afterany` 라 **대기잡을 먼저** 취소해야 했다(73925 를 먼저 죽이면 73926 이 즉시 시작) |
| ~~**1**~~ | ✅ **완료 (08-04)** — 평가 검정력 보강 | n=300 → **전량 1,772**, per-item 저장 → 짝지음. 검출 하한 8.0pp → **실측 1.8~2.3pp** |
| ~~**2**~~ | ❌ **step 1200 판정 — 실행 안 됨** | 도달 전 붕괴. 사전기준(ⓐ ≥+3pp&p<0.05 완주 / ⓑ −1~+3pp 조기중단 / ⓒ <−1pp 즉시중단)은 **재시작 런에 재사용** |
| **3** | **재시작 파라미터 설계** ← **다음 임계경로** | ① `overlong_filter` 검증·해제 ② KL `beta` 0.04 상향 ③ **형식 감시 조기중단**(`FormatThink<0.85` 지속 시) — 절벽 **25 step 전부터 경고가 있었다** ④ `max_grad_norm` 1.0 은 무의미(실측 최대 0.17) |
| **4** | **재시작 지점 선택** | 후보: init / ~400 / **850**. 850 은 홀드아웃 최고(51.52%)이나 **붕괴 직전이라 같은 궤도를 탈 위험**이 있다 |
| 5 | **의료는 Stage-3가 본진** — 근거가 굳었다 | n=100 의 한계가 아니었다. **n=400·여섯 지점 전부 무변화**(p>0.5) |

*기준을 미리 못 박는 이유: 결과를 보고 해석을 고르면 어떤 결론이든 정당화된다.*

---

## 부록 A. 발표 시 주의

1. **"1 epoch 학습"이라고 쓰지 말 것** — 실제로는 0.25 epoch(§5).
2. **구 홀드아웃 수치와 섞지 말 것** — v3 0.348 · dr_grpo 0.380 · GDPO 0.390 · GSPO 0.290 은
   구 홀드아웃(DeepVision 972) 기준이다. §7 은 확장 홀드아웃 전량이라 모집단이 다르다.
   굳이 대응시키면 init 의 deepvision 층 0.356 이 과거 v3 0.348 과 같은 계열이다.
3. **말할 수 있는 것과 없는 것을 구분할 것** (08-04 최종):
   - ✅ 말할 수 있다 — **SFT +20.3pp(p<0.001)** · **RL init→850 +8.18pp(p<0.0001)** ·
     **400→850 +2.48pp(p=0.020)** · **의료 무변화(6지점 전부 p>0.5)** · **step ~900 붕괴**
   - ❌ 말하면 안 된다 — **"RL 은 step 400 이후 평평하다"**. 08-03 판에 있던 문장이고 **철회됐다**.
     300 step 간격에서 미검출이었을 뿐, 450 step 간격에서는 검출된다. **"미검출"과 "차이 없음"은 다르다.**
   - ❌ 말하면 안 된다 — **"Stage-2 RLVR 은 실패했다"**. 붕괴 직전까지 개선 중이었고
     게이트를 넘길 속도였다. 실패한 것은 **안정성**이다.
   - ❌ **외삽 금지** — 관측 구간 밖의 기울기를 직선으로 늘리지 않는다. 이 프로젝트에서
     같은 오류를 **양방향으로 두 번** 냈다(08-03 낙관, 08-04 비관).

## 부록 B. 그림 재생성

```bash
# 학습 곡선 6패널
./bin/python scripts/plot_train_curves.py logs/grpo_adv_73924.log \
    -o docs/assets/stage2_expanded_73924_curves.png --max-steps 2337

# 홀드아웃 전량 짝지음 3패널 (§7) — 지점이 늘면 자동 반영된다
./bin/python scripts/plot_holdout_paired.py logs/eval_items_*.jsonl \
    -o docs/assets/stage2_holdout_paired.png

# (구) n=300 층화 추세 — plot_eval_trend.py 는 소스별 n 을 n/3 으로 가정하므로
#      전량(972/400/400)에는 쓰면 안 된다. 과거 그림 재생성용으로만 남긴다.
./bin/python scripts/plot_eval_trend.py "logs/eval_midtrain_results_7406*.jsonl" \
    --points init:0,step400:400,step500:500,trained:600 --base base \
    -o docs/assets/stage2_holdout_trend.png
```

CJK 폰트가 없는 환경(계산노드)에서는 레이블이 자동으로 영문으로 떨어진다.

## 부록 C. 평가 재현

```bash
# 특정 체크포인트만 (base·init 은 기존 측정값 재사용)
sbatch --partition=debug-1gpu --export=ALL,\
EVAL_STAGES=trained,TRAINED_TAG=step600,EVAL_N=300,EVAL_CONC=16,\
MID_CKPT=$PWD/work/checkpoints/_mideval_snap_step600,\
EVAL_DATA=work/data/stage2_expanded_holdout.jsonl \
  scripts/eval_midtrain.slurm
```

`EVAL_N` 과 `EVAL_SEED`(기본 20260802)가 같으면 동일 슬라이스가 뽑혀 과거 측정과 직접 비교된다.
